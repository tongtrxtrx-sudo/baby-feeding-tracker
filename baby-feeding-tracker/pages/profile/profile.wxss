.empty-state {
  text-align: center;
  padding: 160rpx 40rpx;
}

.empty-icon {
  font-size: 160rpx;
  margin-bottom: 32rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 36rpx;
  font-weight: 700;
  color: #333333;
  margin-bottom: 12rpx;
}

.empty-desc {
  font-size: 26rpx;
  color: #999999;
  margin-bottom: 48rpx;
  font-weight: 500;
}

.add-btn {
  width: 320rpx;
  height: 96rpx;
  line-height: 96rpx;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 48rpx;
}

/* 当前宝宝卡片 */
.current-baby-card {
  background: linear-gradient(135deg, #FFB6C1 0%, #FF6B9D 100%);
  color: white;
  padding: 36rpx 32rpx;
  border-radius: 32rpx;
  overflow: hidden;
  position: relative;
  margin-bottom: 24rpx;
  box-shadow: 0 12rpx 40rpx rgba(255, 107, 157, 0.3);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.welcome-decor {
  position: absolute;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 50%;
}

.welcome-decor.decor-1 {
  top: -100rpx;
  right: -60rpx;
  width: 260rpx;
  height: 260rpx;
}

.welcome-decor.decor-2 {
  top: 120rpx;
  left: 50rpx;
  width: 180rpx;
  height: 180rpx;
}

.welcome-decor.decor-3 {
  bottom: -80rpx;
  right: 80rpx;
  width: 200rpx;
  height: 200rpx;
  background: rgba(255, 255, 255, 0.1);
}

.baby-left {
  position: relative;
  z-index: 1;
  flex: 1;
}

.baby-text {
  position: relative;
  z-index: 1;
  margin-bottom: 28rpx;
}

.baby-name {
  font-size: 52rpx;
  font-weight: 700;
  margin-bottom: 12rpx;
}

.baby-detail {
  color: rgba(255, 255, 255, 0.92);
  font-size: 28rpx;
  line-height: 1.5;
  font-weight: 500;
}

.baby-stats {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: row;
  gap: 20rpx;
}

.stat-item {
  flex: 1;
  text-align: center;
}

.stat-value {
  font-size: 40rpx;
  font-weight: 700;
  margin-bottom: 6rpx;
  line-height: 1.2;
}

.stat-label {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.85);
  font-weight: 500;
}

.baby-right {
  position: relative;
  z-index: 1;
}

.baby-avatar {
  font-size: 120rpx;
}

/* 区块 */
.section {
  margin-top: 24rpx;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.section-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #333333;
}

.add-small-btn {
  font-size: 28rpx;
  color: #FF6B9D;
  background: transparent;
  border: none;
  padding: 0;
  font-weight: 600;
}

/* 宝宝列表 */
.baby-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.baby-item {
  margin-bottom: 0;
  padding: 28rpx 32rpx;
  border-radius: 32rpx;
}

.baby-item-header {
  align-items: flex-start;
}

.baby-item-info {
  flex: 1;
}

.baby-item-name {
  font-size: 34rpx;
  color: #333333;
  margin-bottom: 10rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.baby-item-name.active {
  color: #FF6B9D;
  font-weight: 700;
}

.baby-item-detail {
  font-size: 26rpx;
  color: #999999;
  line-height: 1.4;
  font-weight: 500;
}

.current-badge {
  background: linear-gradient(135deg, #FFB6C1 0%, #FF6B9D 100%);
  color: white;
  font-size: 24rpx;
  padding: 10rpx 28rpx;
  border-radius: 50rpx;
  font-weight: 600;
}

.baby-item-actions {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.action-btns {
  display: flex;
  gap: 12rpx;
}

.action-btn {
  font-size: 24rpx;
  padding: 12rpx 24rpx;
  border-radius: 50rpx;
  font-weight: 600;
}

.action-btn.edit {
  background: #E8F5FF;
  color: #1890FF;
  border: 2rpx solid #1890FF;
}

.action-btn.delete {
  background: #FFF1F0;
  color: #FF4D4F;
  border: 2rpx solid #FF4D4F;
}

/* 弹窗样式 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: flex-end;
}

.modal-content {
  background: #FFFFFF;
  width: 100%;
  border-radius: 40rpx 40rpx 0 0;
  padding: 40rpx;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 0 -12rpx 64rpx rgba(0, 0, 0, 0.18);
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 36rpx;
}

.modal-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #333333;
}

.modal-close {
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: all 0.2s ease;
}

.modal-close:active {
  background: #FFF5F8;
}

.modal-body {
  padding-bottom: 20rpx;
}

.form-item {
  margin-bottom: 28rpx;
}

.form-label {
  font-size: 28rpx;
  color: #666666;
  margin-bottom: 12rpx;
  font-weight: 600;
}

.form-input {
  background: #FFF5F8;
  border-radius: 24rpx;
  padding: 28rpx;
  font-size: 28rpx;
  border: 2rpx solid transparent;
  transition: all 0.3s;
  color: #333333;
}

.form-input:focus {
  background: #FFFFFF;
  border-color: #FF6B9D;
}

.form-picker {
  background: #FFF5F8;
  border-radius: 24rpx;
  padding: 28rpx;
  font-size: 28rpx;
  color: #333333;
  border: 2rpx solid transparent;
  transition: all 0.3s;
}

.form-picker:active {
  background: #F5F5F5;
  border-color: #FF6B9D;
}

/* 性别选择 */
.gender-select {
  gap: 20rpx;
}

.gender-option {
  flex: 1;
  text-align: center;
  padding: 32rpx 20rpx;
  border: 2rpx solid #FFE4E8;
  border-radius: 24rpx;
  font-size: 30rpx;
  font-weight: 600;
  transition: all 0.3s;
  background: #FFFFFF;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.gender-option.active {
  border-color: #FF6B9D;
  background: linear-gradient(180deg, #FFF5F8 0%, #FFE4E8 100%);
  color: #FF6B9D;
  font-weight: 700;
}

.gender-option:active {
  transform: scale(0.96);
}

.modal-footer {
  display: flex;
  gap: 20rpx;
  margin-top: 32rpx;
}

.modal-btn {
  flex: 1;
  height: 96rpx;
  line-height: 96rpx;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 48rpx;
}
